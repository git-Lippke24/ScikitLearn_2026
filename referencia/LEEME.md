# referencia/

Esta carpeta guarda el repositorio original del libro, en **solo lectura**.
No edites nada aquí dentro: se consulta para comparar, no se modifica.

Clónalo desde la raíz del proyecto:

```powershell
git clone https://github.com/gmonce/scikit-learn-book.git referencia/scikit-learn-book
```

Recuerda que su código es de 2013–2015 (Python 2, scikit-learn 0.15) y en su mayoría
no ejecuta. Ver `docs/deprecaciones.md`.
